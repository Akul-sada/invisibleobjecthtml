PHP, HTML, CSS 
1) User enters a login credentials user name, password, hostname and Ip is stored in the database token is generated and stored as cookies fetch index.php (application) from the server.
2) User log out token is deactivated.
3) Token Credits counter
4) User clicks on file upload red box; & upload blue box; & submit; & like button; the session is counted as successful.


